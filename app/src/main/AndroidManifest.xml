<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andreterreiro.lostfound">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".models.objetoPerdido" />
        <activity android:name=".models.objetoEncontrado" />
        <activity android:name=".Register" />
        <activity android:name=".models.Email" />
        <activity android:name=".models.User" />
        <activity android:name=".models.Categoria" />
        <activity android:name=".AddLostObject" />
        <activity android:name=".objRelated" />
        <activity android:name=".AddFoundObj"></activity>
    </application>

</manifest>